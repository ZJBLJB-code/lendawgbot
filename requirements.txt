# TomCash build dependencies
#
# The build is intentionally stdlib-only so the cron stays cheap and reliable.
# Optional extras below are only needed if/when quant-bot adapter is wired in
# CI — pip install will skip them silently if the env marker doesn't match.
#
# pyarrow is required to read trades.parquet emitted by quant-bot. It's heavy
# (~30MB wheel) so we leave it optional. To enable, pin it explicitly:
#   pyarrow>=15
# pyarrow ; python_version >= "3.11"  # uncomment when quant-bot adapter lands
