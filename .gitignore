# Build artifacts
dist/
__pycache__/
*.pyc
.venv/
venv/

# Editor
.DS_Store
.vscode/
.idea/

# Local config (do not check in user-specific paths)
.claude/settings.local.json

# Local sync log
/tmp/lendawgbot-*.log

# Local launchd plist after install (kept .template in repo)
scripts/com.lendawgbot.watcher.plist

# Public-repo PII guard: live JSONL events have account_id = "DUP1234567"
# (the paper trading account). Always exclude them from this repo. See
# scripts/sync_quant_bot.sh — SYNC_SKIP_LIVE=1 default.
data/quant_bot_journal/live/*.jsonl
